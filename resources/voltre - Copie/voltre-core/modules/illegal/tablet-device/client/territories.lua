RegisterNUICallback('illegalDevice:territories:getData', function(_, cb)
    ESX.TriggerServerCallback('voltre:illegalDevice:territories:get', function(result)
        cb(result or { territories = {} })
    end)
end)
